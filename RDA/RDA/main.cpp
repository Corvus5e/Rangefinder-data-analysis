
#include <iostream>
#include <rda\common.h>
#include <rda\vector.h>

#include <rda\io\vizualization.h>

using namespace std;

int main(int argc, char* argv[])
{	
	/*rda::CloudPtr c(new rda::Cloud);
	c->push_back(rda::Point(1, 1, 1));
	c->push_back(rda::Point(10, 10, 10));
	c->push_back(rda::Point(100, 100, 100));

	rda::Vizualizer::init(&argc, argv);
	rda::Vizualizer v;
	v.createWindow("v", 300, 300, 10, 10);
	v.addCloud(c, rda::POINTS, 1.0f, 0.0f, 0.0f, 1.0f, 10.0f);
	rda::Vizualizer::start();*/

	cin.get();
}